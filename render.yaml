services:
  - type: web
    name: ohoster-bot
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:8000"
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /
